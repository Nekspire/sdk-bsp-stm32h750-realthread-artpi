from building import *

cwd = GetCurrentDir()

src = Glob('*.c')
src += Glob('ui/*.c')

path = [cwd]
path += [cwd + '/ui']

group = DefineGroup('File Browser', src, depend = [''], CPPPATH = path)

Return('group')